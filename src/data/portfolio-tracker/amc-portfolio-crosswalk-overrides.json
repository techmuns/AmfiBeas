{
  "meta": {
    "note": "Manual tracker-schemecode → AMC-disclosure overrides for schemes that name-matching can't reach (renames, abbreviations, ambiguous names). An entry takes precedence over the matcher and is recorded with confidence='override'. amcSlug must be the public/amc-holdings slug; amcSchemeCode is that AMC's own scheme code (the sheet identity).",
    "version": 1
  },
  "overrides": {}
}
