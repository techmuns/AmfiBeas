{
  "meta": {
    "version": 1,
    "note": "Manual official-benchmark overrides. Each entry MUST carry a first-party AMC evidence URL and a reason; entries without them are rejected by validate:scheme-benchmarks and never reach the registry. Overrides are applied last and win over scraped evidence for the same scheme. Keep this list small — it is a patch layer, not the architecture.",
    "lastUpdated": null
  },
  "overrides": []
}
